subscription subscription_productionStatusChanged($productionId: Int!) {
  productionStatusChanged(productionId: $productionId) {
    actualCompletion
    currentStage
    estimatedCompletion
    previousStatus
    productionId
    status
    updatedAt
    __typename
  }
}