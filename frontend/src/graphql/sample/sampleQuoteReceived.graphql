subscription subscription_sampleQuoteReceived($sampleId: Int!) {
  sampleQuoteReceived(sampleId: $sampleId) {
    quoteNote
    quotedAt
    quotedBy
    quotedDays
    quotedPrice
    sampleId
    sampleNumber
    __typename
  }
}