subscription subscription_orderStatusChanged($orderId: Int!) {
  orderStatusChanged(orderId: $orderId) {
    orderId
    orderNumber
    previousStatus
    status
    updatedAt
    updatedBy
    __typename
  }
}