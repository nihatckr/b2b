subscription subscription_orderQuoteReceived($orderId: Int!) {
  orderQuoteReceived(orderId: $orderId) {
    orderId
    orderNumber
    quoteNote
    quoteType
    quotedAt
    quotedBy
    quotedDays
    quotedPrice
    __typename
  }
}