# ============================================
# SIGNUP OPERATIONS
# ============================================
# User registration with email, password, name, and account type

mutation Signup($input: SignupInput!) {
  signup(input: $input)
}
